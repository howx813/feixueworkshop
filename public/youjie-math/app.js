'use strict';
const lessons = [
  {name:'均值不等式',en:'INEQUALITIES',category:'不等式 · 最值问题',title:'周长一定，面积何时最大？',description:'从一个可以变形的矩形出发，亲手发现均值不等式里的「相等之美」。',tags:['竞赛入门','约 20 分钟','前置：平方非负'],formula:'(a + b) / 2 ≥ √(ab)',condition:'a、b ≥ 0；当且仅当 a = b 时取等号。',proofTitle:'从「平方非负」出发',proofIntro:'实验能提示结论；下面的证明覆盖所有非负实数 a、b。',steps:[['写下一个永远成立的事实','(√a − √b)² ≥ 0，因为任意实数的平方都非负。'],['展开，再移项','a + b − 2√(ab) ≥ 0，所以 a + b ≥ 2√(ab)。'],['得到均值不等式','两边同除以 2，得到 (a + b) / 2 ≥ √(ab)。'],['找出等号成立的唯一条件','等号等价于 (√a − √b)² = 0，即 a = b。固定 a + b = 10 时，ab ≤ 25，a = b = 5 时达到最大值。']],application:'a + b = 10<br>ab ≤ ((a + b)/2)² = 25',tip:'三个检查：正数、定值、等号',caution:'用均值不等式求最值时，要检查和或积是否为定值，以及等号条件能否在题目范围内取到。',questions:[{tag:'01 / 基础最值',q:'a、b > 0，且 a + b = 12。ab 的最大值是多少？',options:['12','24','36','48'],answer:2,explain:'ab ≤ ((a + b)/2)² = 36；a = b = 6 时等号成立，因此最大值是 36。'},{tag:'02 / 配凑应用',q:'x > 0，x + 9/x 的最小值是多少？',options:['3','6','9','18'],answer:1,explain:'x 与 9/x 都为正，乘积恒为 9，因此和 ≥ 2√9 = 6。x = 9/x，即 x = 3 时取得最小值。'},{tag:'03 / 等号陷阱',q:'0 < x ≤ 1 时，x + 4/x 的最小值是多少？',options:['4，在 x = 2 时取得','5，在 x = 1 时取得','4，但不能取得','不存在最小值'],answer:1,explain:'均值给出的下界 4 不能取到，因为 x = 2 不在范围内。x + 4/x − 5 = (x − 1)(x − 4)/x ≥ 0，所以最小值为 5，在 x = 1 时取得。'}]},
  {name:'柯西不等式',en:'VECTORS & INEQUALITIES',category:'不等式 · 向量视角',title:'同向时，点积才到达上界。',description:'转动一支向量，看长度保持不变时，夹角如何决定点积的大小。',tags:['竞赛进阶','约 20 分钟','前置：向量点积'],formula:'(a² + b²)(c² + d²) ≥ (ac + bd)²',condition:'a、b、c、d 为实数；等号当且仅当 ad = bc。',proofTitle:'把复杂的差，变成一个平方',proofIntro:'二维柯西不等式既能用代数证明，也能从向量夹角理解。',steps:[['先看左右两边相差多少','(a² + b²)(c² + d²) − (ac + bd)²。'],['展开并消去相同的项','差 = a²d² + b²c² − 2abcd = (ad − bc)² ≥ 0。'],['得到结论与等号条件','因此 (a² + b²)(c² + d²) ≥ (ac + bd)²；当且仅当 ad = bc 时取等号。'],['回到向量图像','对非零向量 u、v，u·v = |u||v|cosθ。|cosθ| ≤ 1，所以 |u·v| ≤ |u||v|；同向或反向都使平方形式取等号。']],application:'(x + y)² ≤ 2(x² + y²)<br>x + y = 6 ⇒ x² + y² ≥ 18',tip:'平方形式，同向与反向都能取等号',caution:'点积的最大值出现在同向；点积绝对值的最大值也可出现在反向。零向量同样满足等号，不能用除以坐标的方式漏掉它。',questions:[{tag:'01 / 平方和最值',q:'实数 x、y 满足 x + y = 6，x² + y² 的最小值为？',options:['9','12','18','36'],answer:2,explain:'由 (x + y)² ≤ 2(x² + y²)，得 x² + y² ≥ 18。在 x = y = 3 时取等号。'},{tag:'02 / 线性式最值',q:'x² + y² = 1 时，3x + 4y 的最大值为？',options:['1','5','7','25'],answer:1,explain:'(3x + 4y)² ≤ (3² + 4²)(x² + y²) = 25，故 3x + 4y ≤ 5；x = 3/5、y = 4/5 时取得。'},{tag:'03 / 理解等号',q:'u、v 为非零向量。|u·v| = |u||v| 的条件是？',options:['只需长度相同','只能同向','同向或反向','相互垂直'],answer:2,explain:'|u·v| = |u||v||cosθ|，等号要求 |cosθ| = 1，即 θ = 0° 或 180°。'}]},
  {name:'抽屉原理',en:'COMBINATORICS',category:'组合数学 · 存在性证明',title:'再放一个，必有一格装不下。',description:'把圆点放进抽屉，用最均匀的分配方式，探索「总有一个」背后的必然性。',tags:['竞赛入门','约 20 分钟','前置：整数与余数'],formula:'至少一个抽屉中有 ⌈N / k⌉ 个物体',condition:'N 个物体放入 k 个抽屉，N、k 为正整数；⌈ ⌉ 表示向上取整。',proofTitle:'假设每个抽屉都没有那么多',proofIntro:'关键是把模糊的「必然有」变成对总数的严格限制。',steps:[['定义要保证的数量','设 m = ⌈N/k⌉，也就是不小于 N/k 的最小整数。'],['反过来假设结论不成立','如果没有任何抽屉达到 m 个，那么每个抽屉至多有 m − 1 个。'],['计算最多能装多少','k 个抽屉总共至多装 k(m − 1) 个。因为 m − 1 < N/k，所以 k(m − 1) < N，与实际有 N 个矛盾。'],['理解「至少」与「恰好」','因此总有一个抽屉至少装 m 个。均匀分配能让最大装载量恰为 m；不均匀分配的最大装载量可能更大。']],application:'保证有 r 个在同一抽屉<br>最少需要 k(r − 1) + 1 个',tip:'先定义清楚「物体」和「抽屉」',caution:'它保证至少有一个抽屉达到数量，并不保证每个抽屉都有这么多，也不指定是哪一个抽屉。实验中的均匀分配只是所有分配中的一种。',questions:[{tag:'01 / 直接应用',q:'13 位同学中，一定至少有多少人的生日在同一个月份？',options:['1 人','2 人','3 人','无法保证'],answer:1,explain:'把 12 个月视为 12 个抽屉，13 位同学视为物体。⌈13/12⌉ = 2，因此至少有 2 人的生日在同一月份。'},{tag:'02 / 反向计数',q:'要保证有 5 人的生日在同一月份，至少需要多少人？',options:['48 人','49 人','60 人','61 人'],answer:1,explain:'每月最多 4 人时可以容纳 12×4 = 48 人。第 49 人必使某个月达到 5 人；48 人可均分，不能保证。'},{tag:'03 / 构造抽屉',q:'任取 6 个整数，一定能找到两个数，使它们的差是哪个数的倍数？',options:['5','6','7','10'],answer:0,explain:'整数除以 5 的余数只有 0、1、2、3、4 五种。6 个整数中至少两者余数相同，它们的差就是 5 的倍数。'}]}
];
const $ = s => document.querySelector(s);
const storeKey = 'youjie-math-lab-v1';
let answers = {}, storageAvailable = true;
try { const saved = JSON.parse(localStorage.getItem(storeKey) || '{}'); if(saved && typeof saved === 'object' && !Array.isArray(saved)) answers = saved; } catch { storageAvailable = false; }
let lesson = 0, stage = 0, proofCount = 1, a = 3, angle = 45, total = 9, layout = 'balanced';
let selections = {}, checked = {}, timer = null, toastTimer;
const stageNames = ['探索实验','推导证明','闯关练习'];
function toast(text){$('#toast').textContent=text;$('#toast').classList.add('visible');clearTimeout(toastTimer);toastTimer=setTimeout(()=>$('#toast').classList.remove('visible'),2800)}
function score(){return lessons.reduce((n,l,i)=>n+l.questions.filter((_,j)=>answers[`${i}-${j}`]===true).length,0)}
function updateProgress(){ $('#progress-count').textContent=`${score()} / 9`;$('#progress').value=score();if(!storageAvailable)$('.progress-card small').textContent='当前浏览器未允许保存，进度仅本次有效';$('#course-nav').innerHTML=lessons.map((l,i)=>`<button class="course-button ${i===lesson?'active':''}" data-lesson="${i}" ${i===lesson?'aria-current="page"':''}><span class="course-number">0${i+1}</span><span><strong>${l.name}</strong><small>${l.category}</small></span><span class="course-dot">${l.questions.every((_,j)=>answers[`${i}-${j}`]===true)?'✓':i===lesson?'↗':'·'}</span></button>`).join(''); }
function save(){try{localStorage.setItem(storeKey,JSON.stringify(answers))}catch{storageAvailable=false}updateProgress()}
function stopAnimation(){if(timer){cancelAnimationFrame(timer);timer=null}}
function render(){stopAnimation();const l=lessons[lesson];updateProgress();$('#crumb').textContent=l.name;$('#eyebrow').textContent=`LESSON 0${lesson+1} / ${l.en}`;$('#lesson-title').textContent=l.title;$('#lesson-description').textContent=l.description;$('#lesson-meta').innerHTML=l.tags.map((t,i)=>`<span class="chip ${i===0?'level':''}">${i===0?'◈':i===1?'◷':'·'} ${t}</span>`).join('');$('#stage-tabs').innerHTML=stageNames.map((n,i)=>`<button class="stage-tab ${i===stage?'active':''}" role="tab" id="tab-${i}" aria-controls="lesson-body" aria-selected="${i===stage}" data-stage="${i}"><span>0${i+1}</span>${n}</button>`).join('');$('#lesson-body').setAttribute('role','tabpanel');$('#lesson-body').setAttribute('aria-labelledby',`tab-${stage}`);$('#stage-hint').textContent=['每一个猜想，都从一次尝试开始','从一个例子，走向所有情形','检验理解，也检验等号条件'][stage];$('#footer-copy').textContent=['试着找到边界，再问一问：为什么一定成立？','想清楚适用范围与等号条件，再进入练习。',`本课已掌握 ${l.questions.filter((_,j)=>answers[`${lesson}-${j}`]===true).length} / 3 题 · 可重复作答巩固理解`][stage];$('#next-stage').innerHTML=stage===0?'进入推导 <span>→</span>':stage===1?'开始闯关 <span>→</span>':lesson<2?'探索下一课 <span>→</span>':'下载学习报告 <span>↓</span>';if(stage===0)renderExperiment();else if(stage===1)renderProof();else renderQuiz();}
function switchStage(s){stage=s;render()}
function experimentShell(title,caption,control,observation,formula){return `<div class="experiment"><div class="canvas-panel"><div class="canvas-header"><strong>${title}</strong><span class="live-badge">● LIVE EXPERIMENT</span></div><svg id="experiment-svg" viewBox="0 0 580 290" role="img" aria-label="${title}"></svg><div class="canvas-caption">${caption}</div></div><aside class="controls"><div class="control-top"><h2>实验控制台</h2><button class="reset-exp" id="reset-exp" aria-label="重置实验" title="重置实验">↺</button></div>${control}</aside></div><div class="observation"><span class="observation-icon">✧</span><div><strong id="observation-title">观察一下</strong><p id="observation-text">${observation}</p></div><div class="formula">${formula}</div></div>`}
function renderExperiment(){
  const control=lesson===0?`<p class="control-description">固定周长为 20，改变矩形的形状。</p><label class="slider-label" for="parameter">边长 <span class="math">a</span><output id="parameter-value"></output></label><input id="parameter" type="range" min="0.5" max="9.5" step="0.1" value="${a}"><div class="range-ends"><span>0.5</span><span>9.5</span></div><div class="parameter">另一条边 b = 10 − a<strong id="b-value"></strong></div><div class="metric"><div class="metric-label">当前面积 S = a × b</div><div class="metric-number" id="metric-value"></div><div class="meter"><div id="metric-bar"></div></div><div class="metric-bottom"><span>0</span><span>理论上界 25</span></div></div><button class="secondary-button" id="balance">◎ 找到最大面积</button>`:lesson===1?`<p class="control-description">固定 |u| = 4，|v| = 3，转动向量 v。</p><label class="slider-label" for="parameter">向量夹角 θ<output id="parameter-value"></output></label><input id="parameter" type="range" min="0" max="180" step="1" value="${angle}"><div class="range-ends"><span>0° 同向</span><span>180° 反向</span></div><div class="parameter">长度乘积 |u| × |v|<strong>12</strong></div><div class="metric"><div class="metric-label">当前点积 u · v</div><div class="metric-number" id="metric-value"></div><div class="meter"><div id="metric-bar"></div></div><div class="metric-bottom"><span>绝对值 0</span><span>绝对值上界 12</span></div></div><button class="secondary-button" id="balance">◎ 转到同向</button>`:`<p class="control-description">固定 4 个抽屉，增加圆点的数量。</p><label class="slider-label" for="parameter">圆点总数 N<output id="parameter-value"></output></label><input id="parameter" type="range" min="1" max="20" step="1" value="${total}"><div class="range-ends"><span>1 个</span><span>20 个</span></div><div class="parameter">抽屉数 k<strong>4</strong></div><div class="metric"><div class="metric-label">必然存在的最少装载数 ⌈N/4⌉</div><div class="metric-number" id="metric-value"></div><div class="metric-bottom"><span id="allocation-label"></span></div></div><div class="pigeon-controls"><button class="secondary-button" id="balanced">均匀分配</button><button class="secondary-button" id="concentrated">集中到一格</button></div>`;
  $('#lesson-body').innerHTML=experimentShell(['定周长矩形 · 面积实验','向量夹角 · 点积实验','四个抽屉 · 分配实验'][lesson],['拖动滑块或右下角圆点，观察矩形面积的变化','拖动滑块或紫色向量端点，观察点积与夹角','两种分配方式，都能验证「至少一个抽屉」的保证'][lesson],control,'',['a = b 时，ab 最大','|u · v| ≤ |u| |v|','⌈N / 4⌉'][lesson]);
  $('#parameter').addEventListener('input',e=>{stopAnimation();if(lesson===0)a=Number(e.target.value);else if(lesson===1)angle=Number(e.target.value);else total=Number(e.target.value);draw()});
  $('#reset-exp').onclick=()=>{stopAnimation();if(lesson===0)a=3;else if(lesson===1)angle=45;else {total=9;layout='balanced'}renderExperiment()};
  if(lesson<2)$('#balance').onclick=animateBalance;else {$('#balanced').onclick=()=>{layout='balanced';draw()};$('#concentrated').onclick=()=>{layout='concentrated';draw()};}
  draw();
  const svg=$('#experiment-svg');let dragging=false;
  svg.addEventListener('pointerdown',e=>{if(e.target.id==='drag-handle'){dragging=true;svg.setPointerCapture(e.pointerId)}});
  svg.addEventListener('pointermove',e=>{if(!dragging)return;stopAnimation();const pt=new DOMPoint(e.clientX,e.clientY).matrixTransform(svg.getScreenCTM().inverse());if(lesson===0)a=Math.max(.5,Math.min(9.5,Math.round((pt.x-90)/24*10)/10));else if(lesson===1)angle=Math.max(0,Math.min(180,Math.round(Math.atan2(204-pt.y,pt.x-290)*180/Math.PI)));draw()});
  ['pointerup','pointercancel','lostpointercapture'].forEach(name=>svg.addEventListener(name,()=>dragging=false));
}
function animateBalance(){stopAnimation();const from=lesson===0?a:angle,to=lesson===0?5:0;if(matchMedia('(prefers-reduced-motion: reduce)').matches){if(lesson===0)a=5;else angle=0;draw();return}const start=performance.now();function frame(now){const t=Math.min(1,(now-start)/650),eased=1-Math.pow(1-t,3);if(lesson===0)a=Math.round((from+(to-from)*eased)*10)/10;else angle=Math.round(from+(to-from)*eased);draw();if(t<1)timer=requestAnimationFrame(frame);else timer=null}timer=requestAnimationFrame(frame)}
function draw(){
 const svg=$('#experiment-svg');if(!svg)return;
 const text=(x,y,s,fill='#7c879d',size=12,extra='')=>`<text x="${x}" y="${y}" fill="${fill}" font-size="${size}" text-anchor="middle" font-family="Georgia, 'Songti SC', serif" ${extra}>${s}</text>`;
 if(lesson===0){const b=10-a,area=a*b;const equal=Math.abs(a-5)<.01;$('#parameter').value=a;$('#parameter-value').textContent=a.toFixed(1);$('#b-value').textContent=b.toFixed(1);$('#metric-value').innerHTML=`${area.toFixed(2)}<small>平方单位</small>`;$('#metric-bar').style.width=`${area/25*100}%`;svg.setAttribute('aria-label',`边长 ${a.toFixed(1)} 与 ${b.toFixed(1)} 的矩形，面积 ${area.toFixed(2)}，最大值 25`);
 $('#observation-title').textContent=equal?'你找到了等号成立的时刻':'离「相等」越近，面积越大';$('#observation-text').textContent=equal?'a = b = 5，面积达到 25。怎样证明其他形状都不会超过它？':`当前面积距离上界还差 ${(25-area).toFixed(2)}。保持 a + b = 10，把两条边调得更接近试试。`;
 const rw=a*24,rh=b*24;
 svg.setAttribute('viewBox','0 0 580 320');
 svg.innerHTML=`<defs><pattern id="tile" width="24" height="24" patternUnits="userSpaceOnUse"><path d="M24 0H0V24" fill="none" stroke="#bcc7ed" stroke-width=".7"/></pattern></defs><rect x="90" y="30" width="120" height="120" fill="none" stroke="#9faecb" stroke-dasharray="5 5"/><rect x="90" y="30" width="${rw}" height="${rh}" fill="${equal?'#def0e8':'#e3e8ff'}"/><rect x="90" y="30" width="${rw}" height="${rh}" fill="url(#tile)" stroke="${equal?'#248575':'#6375d9'}" stroke-width="2"/>${rw>72&&rh>38?text(90+rw/2,30+rh/2+6,`${area.toFixed(1)}`,equal?'#248575':'#5365db',24):''}<path d="M90 ${30+rh+18}v6 M90 ${30+rh+21}H${90+rw} M${90+rw} ${30+rh+18}v6" stroke="#8694b2" fill="none"/>${text(90+rw/2,30+rh+42,`a = ${a.toFixed(1)}`,'#5365db',15)}${text(90+rw+42,30+rh/2+5,`b = ${b.toFixed(1)}`,'#5365db',15)}<circle id="drag-handle" cx="${90+rw}" cy="${30+rh}" r="9" fill="#5365db" stroke="white" stroke-width="3" style="cursor:ew-resize"/><g transform="translate(430 54)"><rect x="-60" y="-25" width="127" height="61" rx="7" fill="white" stroke="#e1e6f0"/>${text(4,-1,'a + b = 10','#263959',19)}${text(4,20,'周长始终是 20','#7c879d',10)}</g><g transform="translate(425 171)">${text(0,0,'S ≤ 25','#a3aec4',29)}${text(0,24,'面积的上界','#98a4ba',10)}</g>${text(420,255,'虚线：a = b = 5 的正方形','#98a4ba',10)}`;
 }else if(lesson===1){const r=angle*Math.PI/180,c=Math.cos(r),s=Math.sin(r),vx=290+144*c,vy=204-144*s,dot=12*c;$('#parameter').value=angle;$('#parameter-value').textContent=`${angle}°`;$('#metric-value').innerHTML=`${Math.abs(dot)<.00001?'0.00':dot.toFixed(2)}<small>12 cos θ</small>`;$('#metric-bar').style.width=`${Math.abs(c)*100}%`;svg.setAttribute('aria-label',`向量夹角 ${angle} 度，点积 ${dot.toFixed(2)}`);svg.innerHTML=`<defs><marker id="arrow-u" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="5" markerHeight="5" orient="auto"><path d="M0 0L10 5L0 10" fill="#248575"/></marker><marker id="arrow-v" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="5" markerHeight="5" orient="auto"><path d="M0 0L10 5L0 10" fill="#5365db"/></marker></defs><path d="M146 204A144 144 0 0 1 434 204" fill="none" stroke="#cbd4e6" stroke-dasharray="5 5"/><path d="M100 204H510 M290 35V239" fill="none" stroke="#dde3ef"/><path d="M290 204L${vx} ${vy}L${vx} 204Z" fill="#5365db10"/><path d="M${vx} ${vy}V204" stroke="#acb7e2" stroke-dasharray="4 4"/><path d="M290 204H482" stroke="#248575" stroke-width="3" marker-end="url(#arrow-u)"/><path d="M290 204L${vx} ${vy}" stroke="#5365db" stroke-width="3" marker-end="url(#arrow-v)"/><circle cx="290" cy="204" r="4" fill="#202e48"/><circle id="drag-handle" cx="${vx}" cy="${vy}" r="9" fill="#5365db" stroke="white" stroke-width="3" style="cursor:grab"/>${text(484,231,'u · 长度 4','#248575',14)}${text(vx,vy-19,'v · 长度 3','#5365db',14)}${text(278,231,'O','#7c879d',13)}${text(365,268,`θ = ${angle}°`,'#5365db',20)}${text(140,57,'u · v = 12 cos θ','#98a4ba',17)}`;$('#observation-title').textContent=angle===0?'同向：点积达到最大值':angle===180?'反向：点积最小，绝对值仍取等号':angle===90?'垂直：点积恰好为零':'夹角改变点积，长度乘积保持不变';$('#observation-text').textContent=`当前点积为 ${dot.toFixed(2)}，绝对值为 ${Math.abs(dot).toFixed(2)}，始终不超过 12。试试 0°、90° 和 180°。`;
 }else{const counts=layout==='balanced'?Array.from({length:4},(_,i)=>Math.floor(total/4)+(i<total%4?1:0)):[total,0,0,0];const m=Math.ceil(total/4),mx=Math.max(...counts);$('#parameter').value=total;$('#parameter-value').textContent=total;$('#metric-value').innerHTML=`${m}<small>个圆点</small>`;$('#allocation-label').textContent=`当前最多的一格：${mx} 个 ≥ ${m} 个`;$('#balanced').setAttribute('aria-pressed',layout==='balanced');$('#concentrated').setAttribute('aria-pressed',layout==='concentrated');$('#balanced').style.background=layout==='balanced'?'#dfe4ff':'#fff';$('#concentrated').style.background=layout==='concentrated'?'#dfe4ff':'#fff';svg.setAttribute('aria-label',`${total} 个圆点放入 4 个抽屉，当前每格分别有 ${counts.join('、')} 个，必有一格至少 ${m} 个`);svg.innerHTML=counts.map((n,i)=>{const x=39+i*131;return `<rect x="${x}" y="45" width="110" height="182" rx="12" fill="${n===mx?'#edf0ff':'#fff'}" stroke="${n===mx?'#8d9be8':'#dce2ef'}"/>${Array.from({length:n},(_,j)=>`<circle cx="${x+22+(j%4)*22}" cy="${74+Math.floor(j/4)*27}" r="7" fill="${n===mx?'#6677db':'#a9b4d4'}"/>`).join('')}${text(x+55,251,`${String.fromCharCode(65+i)} 抽屉`,'#7c879d',12)}${text(x+55,211,`${n} 个`,n===mx?'#5365db':'#7c879d',17)}`}).join('');$('#observation-title').textContent=layout==='balanced'?'即使尽量平均，也逃不开这个下界':'不均匀分配，最大的一格只会更多';$('#observation-text').textContent=`${total} 个圆点，4 个抽屉，至少有一个抽屉装 ${m} 个。${total%4===1&&total>1?'刚超过 4 的倍数，保证的数量就增加了 1。':'这个结论对所有分配方式都成立。'}`;}
}
function renderProof(){const l=lessons[lesson];$('#lesson-body').innerHTML=`<div class="proof-layout"><section class="proof-main"><h2>${l.proofTitle}</h2><p>${l.proofIntro}</p><div class="theorem"><span class="math">${l.formula}</span><small>${l.condition}</small></div><div id="proof-steps">${l.steps.slice(0,proofCount).map((s,i)=>`<div class="proof-step"><span class="step-index">${i+1}</span><div><strong>${s[0]}</strong><p>${s[1]}</p></div></div>`).join('')}</div><button class="secondary-button" id="reveal-proof">${proofCount<4?`展开下一步 · ${proofCount} / 4 ↓`:'↺ 重新推导'}</button></section><aside class="proof-aside"><div class="eyebrow">TRANSFER / 迁移</div><h3 style="margin-top:18px">把结论带进题目</h3><div class="application math">${l.application}</div><p>${lesson===0?'定和求积的最大值，是均值不等式最直接的应用。':lesson===1?'将 (x, y) 与 (1, 1) 配对；等号在 x = y 时成立。':'先让每个抽屉装 r − 1 个，再增加一个就必然突破。'}</p><div class="caution"><strong>${l.tip}</strong><p>${l.caution}</p></div></aside></div>`;$('#reveal-proof').onclick=()=>{proofCount=proofCount<4?proofCount+1:1;renderProof()};}
function renderQuiz(){const l=lessons[lesson];$('#lesson-body').innerHTML=`<div class="quiz-header"><h2>把刚刚的发现，变成你的解法。</h2><span>3 道题 · 即时解析</span></div><div class="questions">${l.questions.map((q,j)=>{const key=`${lesson}-${j}`,sel=selections[key],isChecked=checked[key],right=isChecked&&sel===q.answer;return `<section class="quiz-card"><div class="question-tag">${q.tag} ${answers[key]===true?'· ✓ 已掌握':''}</div><h3>${q.q}</h3><div class="options" role="group" aria-label="第 ${j+1} 题选项">${q.options.map((o,k)=>`<button class="option ${sel===k?'selected':''} ${isChecked&&k===q.answer?'correct':''} ${isChecked&&sel===k&&!right?'wrong':''}" data-question="${j}" data-choice="${k}" aria-pressed="${sel===k}"><span>${'ABCD'[k]}</span>${o}</button>`).join('')}</div><button class="primary-button check-answer" data-check="${j}" ${sel===undefined?'disabled':''}>${isChecked?'重新作答':'检查答案'}</button>${isChecked?`<p class="feedback ${right?'':'retry'}" role="status"><strong>${right?'✓ 回答正确':'再想一步 · 正确答案 '+ 'ABCD'[q.answer]}</strong>${q.explain}</p>`:''}</section>`}).join('')}</div><div class="report-row"><span>已掌握 ${score()} / 9 题。答错后可重新作答；答对记录会保留。</span><button class="secondary-button" id="download-report">↓ 下载学习报告</button></div>`;document.querySelectorAll('[data-choice]').forEach(b=>b.onclick=()=>{const key=`${lesson}-${b.dataset.question}`;if(checked[key])return;selections[key]=Number(b.dataset.choice);renderQuiz()});document.querySelectorAll('[data-check]').forEach(b=>b.onclick=()=>{const j=Number(b.dataset.check),key=`${lesson}-${j}`;if(checked[key]){delete checked[key];delete selections[key];renderQuiz();return}checked[key]=true;if(selections[key]===l.questions[j].answer){answers[key]=true;save()}renderQuiz();$('#footer-copy').textContent=`本课已掌握 ${l.questions.filter((_,i)=>answers[`${lesson}-${i}`]===true).length} / 3 题 · 可重复作答巩固理解`;});$('#download-report').onclick=downloadReport;}
function downloadReport(){const date=new Date().toLocaleString('zh-CN');const html=`<!doctype html><html lang="zh-CN"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>有解 · 学习报告</title><style>body{max-width:780px;margin:50px auto;padding:0 24px;font:15px/1.9 system-ui;color:#202e48}h1{font-size:30px}h2{color:#5365db;border-top:1px solid #ddd;padding-top:25px}p{color:#69768f}li{padding:7px 0}small{color:#8993a4}</style><h1>有解 · 高中奥数学习报告</h1><p>生成时间：${date}<br>已掌握 ${score()} / 9 题；正确率未统计，掌握状态表示曾答对。</p>${lessons.map((l,i)=>`<h2>0${i+1} ${l.name}</h2><p>${l.formula}<br>${l.condition}</p><ol>${l.questions.map((q,j)=>`<li><strong>${answers[`${i}-${j}`]===true?'✓ 已掌握':'○ 待巩固'}</strong> · ${q.q}<br><small>解法：${q.explain}</small></li>`).join('')}</ol>`).join('')}<p>建议：对待巩固题目返回相应推导，检查适用条件，再独立作答。</p></html>`;const url=URL.createObjectURL(new Blob([html],{type:'text/html;charset=utf-8'}));const link=document.createElement('a');link.href=url;link.download='有解-高中奥数学习报告.html';document.body.append(link);link.click();link.remove();setTimeout(()=>URL.revokeObjectURL(url),3000);toast('学习报告已生成，可在下载文件中打开');}
function buildHandout(){$('#print-handout').innerHTML=`<h1>有解 · 高中奥数交互课讲义</h1><p>三课教学包 / 高一至高二竞赛启蒙 / 每课建议 20 分钟</p><p>姓名：________________　日期：________________</p><h3>学习路线</h3><p>① 动手实验，提出猜想。② 分步推导，检查等号。③ 独立练习，迁移方法。</p><p>先修知识：平方非负、平方根、平面向量点积、整数余数。学习目标：能解释三条基本原理，能检查适用条件，并能用于简单最值与存在性问题。</p><h3>课堂安排</h3><p>探索 5 分钟：先预测，再操作，记录边界。证明 7 分钟：每揭示一步前，请学生先尝试推理。练习 8 分钟：独立作答，说明等号条件或抽屉构造。</p>${lessons.map((l,i)=>`<section class="print-lesson"><h2>0${i+1} / ${l.name}</h2><p>${l.description}</p><div class="print-formula">${l.formula}</div><p>${l.condition}</p><h3>实验记录</h3><p>${['固定 a + b = 10，分别记录 a = 1、3、5、7、9 时的面积。面积与 a − b 的大小有什么关系？','固定两向量长为 4、3，记录 θ = 0°、45°、90°、135°、180° 的点积。同向与反向有什么区别？','固定 4 个抽屉，记录 N = 4、5、8、9 时的保证数量，并比较均匀分配与集中分配。'][i]}</p><p>我的发现：________________________________________________</p><h3>证明要点</h3><ol>${l.steps.map(s=>`<li><strong>${s[0]}</strong>：${s[1]}</li>`).join('')}</ol><p><strong>易错提醒：</strong>${l.caution}</p><h3>独立练习</h3><ol>${l.questions.map(q=>`<li>${q.q}<br>${q.options.map((o,j)=>`${'ABCD'[j]}. ${o}`).join('　')}</li>`).join('')}</ol></section>`).join('')}<section class="print-lesson answer-key"><h2>教师参考答案</h2>${lessons.map(l=>`<h3>${l.name}</h3><ol>${l.questions.map(q=>`<li><strong>${'ABCD'[q.answer]}</strong>：${q.explain}</li>`).join('')}</ol>`).join('')}</section>`}
$('#course-nav').onclick=e=>{const b=e.target.closest('[data-lesson]');if(!b)return;lesson=Number(b.dataset.lesson);stage=0;proofCount=1;render()};
$('#stage-tabs').onclick=e=>{const b=e.target.closest('[data-stage]');if(b)switchStage(Number(b.dataset.stage))};
$('#stage-tabs').onkeydown=e=>{if(e.key!=='ArrowRight'&&e.key!=='ArrowLeft')return;e.preventDefault();switchStage((stage+(e.key==='ArrowRight'?1:2))%3);$(`#tab-${stage}`).focus()};
$('#next-stage').onclick=()=>{if(stage<2){stage++;render()}else if(lesson<2){lesson++;stage=0;proofCount=1;render()}else downloadReport()};
$('#guide').onclick=()=>$('#guide-dialog').showModal();document.querySelectorAll('.close-dialog').forEach(b=>b.onclick=()=>$('#guide-dialog').close());
$('#reset').onclick=()=>{if(confirm('清除这台浏览器上的全部 9 题学习记录？')){answers={};selections={};checked={};save();render();toast('学习记录已重置')}};
$('#present').onclick=()=>{const on=document.body.classList.toggle('presenting');$('#present').innerHTML=on?'⊡ <span>退出投屏</span>':'⛶ <span>课堂投屏</span>';$('#present').setAttribute('aria-pressed',on);if(on)toast('已进入投屏布局，← / → 切换环节，Esc 退出')};
document.addEventListener('keydown',e=>{if($('#guide-dialog').open||e.target.matches('input,select,textarea')||e.target.closest('[role=tablist]'))return;if(e.key==='Escape'&&document.body.classList.contains('presenting'))$('#present').click();if(document.body.classList.contains('presenting')&&(e.key==='ArrowRight'||e.key==='ArrowLeft')){e.preventDefault();switchStage(Math.max(0,Math.min(2,stage+(e.key==='ArrowRight'?1:-1))))}});
$('#print').onclick=()=>{buildHandout();window.print()};
buildHandout();render();
